#include "./include/scheduler.h"

uint64_t scheduler(uint64_t current_sp){
    //PCB* current_process = get_current_p();
    //Hacer la logica de elegir el proximo procesa a correr basicamente
    return current_sp;
}